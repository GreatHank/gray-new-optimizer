# gray_new

基于固定物理前向模型的多通道衍射灰度优化研究项目，用共享的二维相位梯度生成连续级次方阵中的多通道目标图案；默认仍为第一象限36级次，也支持显式平移级次起点。

## 背景与目标

项目面向几何相位超表面/远场全息设计。当前物理配置使用第一象限 `m,n∈{1,…,6}` 的36个级次，验证共享相位下的多通道成像、灰度分级和跨通道亮度一致性，并为后续相位导出、版图生成和 FDTD 验证提供数据。

适用场景包括算法实验、不同损失权重对比、结果可视化，以及由相位矩阵生成 CIF 掩模。

## 当前能力

- 提供本地“级次视场工作台”：选择浏览器可读取的本地图片后，按完全平方数 channel 等分，将每个切片按原图方向拼接为浅色级次底图；Channel 数使用仅含 `4、9、16…400` 的完全平方数下拉框，不接受任意数字。支持共同入射角滑动/键入、一键反算 `theta/phi` 自动居中、物理参数联动、FoV 与传播/NA 统计、PNG 导出，并同时枚举空气圆内所有未选择候选级次及边缘部分相交的局部角谱块。数值输入采用短延迟更新，全视场扫描超过2500格时会明确报错，以免极小 `lambda/P` 在输入过程中卡死页面。默认沿用 `easy_picture` 的 3×3 参数。详见 [级次选择可视化网站](doc/order-visualizer-site.md)。
- 已按公开补充材料复算李仲阳团队大视场论文实例：23通道参数可严格复现23/23传播及Table S3出射角；Sample 3的`87°`可解释为未占满完整3×3角谱区域后的实际FoV；`168°`和近`180°`实例因公开参数不完整而明确标为不可严格复跑。详见 [论文实例参数复算](doc/li-zhongyang-paper-example-reproduction.md)。
- 针对 `input/easy_picture.png` 完成 Figure S9 风格连续3×3“钟表式”级次搜索：整幅图只做identity/旋转/镜像后切块，严格排除 `(0,0)`，并输出复杂度排名、共同入射传播表、局部角谱方格余量及9-channel MAT。详见 [easy_picture 3×3级次分析](doc/easy-picture-3x3-clock-order-analysis.md)。

- 从 MAT 文件读取1–49个正方形通道、标签为 `0/1` 或 `0、1/3、2/3、1` 的目标 `bw_all`。
- 6×6图像目标可按固定 `85/170/255` 灰度映射，并彻底排除纯黑网格；MAT中的 `grid_positions` 保存有效channel的原始位置，完整场景预览时再补回黑格。
- 优化共享相位变量 `dx/dy`，按可配置起点的连续6×6级次矩阵重建最多36通道远场强度。
- 可选跨通道亮区一致性损失与最差通道惩罚。
- 支持固定灰度比例、跨通道灰度一致性、线内均匀性、背景空间均匀性和全背景亮度带损失。
- 支持不区分方向的背景局部亮度集中损失，可用不同邻域尺度抑制背景亮斑成块。
- 默认使用总能量归一化的灰度分布损失，使稀疏水果目标按 `1:2:3` 分配衍射能量；旧的总 MSE 和前景/背景分区 MSE 仍可显式选择用于对照。
- 支持逐目标像素最低响应损失，并以灰度线自身均值归一化线内方差，用于减少缺线和线内斑驳。
- 输出优化结果、逐通道指标和目标/结果对比图。
- 每次优化自动输出结构完整、灰度分级、同灰度跨通道一致性、背景均匀性和前景/背景SNR-like五类评价，并生成逐通道与汇总 CSV。
- SNR评价采用论文口径`20log10(mu_fg/sigma_bg)`；背景扣除的`(mu_fg-mu_bg)/sigma_bg`单独保留为CNR和训练代理。
- 支持用尺度不敏感的可微CNR损失直接拉开前景/背景，并与背景方差、亮点上限和局部亮团损失联合抑制散斑。
- 可在优化前评价四灰度源图的逐channel目标总亮度、三档前景灰阶均值、线条覆盖率、灰阶信息量和边界复杂度，用于筛选更适合共享相位优化的输入图。
- 提供权重计算、已有 NPZ 结果可视化及 MATLAB CIF 掩模生成辅助脚本。
- 提供单一全局光强倍率测试，验证所有channel同步增亮时绝对强度线性增加，而CV和相对灰度比例保持不变。
- 支持将36通道按6×6空间顺序重排为完整场景，并用统一物理曝光生成原图/优化图左右对比及多阶段总览。
- 支持把已切分的7×7四灰度圆形场景逐块最近邻缩小为49级次目标；纯黑角块排除后按原网格坐标映射物理级次。
- 提供二维光栅方程级次验证器，可区分固定入射的大视场角域拼接与逐级次独立居中两种模式；支持反算中心入射角，并报告零级、正负共轭和整数谐波关系。
- 支持把固定 `0/85/170/255` 圆形场景整幅等分为完整6×6目标，并对指定 `n=0` 列输出逐级次结构、覆盖率、亮度和统一曝光图。
- 支持将固定四灰度蝴蝶图整体旋转180°后切成连续4×4目标，并在完整场景重排后统一逆向旋转恢复原方向。
- 支持将固定四灰度falcons图保持identity方向切成完整5×5目标，并映射到`m=-4…0、n=1…5`。

## 技术栈与环境

- Python 3
- 原生 HTML、CSS、JavaScript（本地级次可视化网站，无前端依赖）
- NumPy、SciPy、PyTorch、Matplotlib
- MATLAB（仅 `mask_generate.m`）
- CUDA 可选；主脚本也支持 CPU

本项目目前没有锁定依赖版本或自动安装脚本。历史验证环境为 Python 3.13.1、NumPy 2.2.1、SciPy 1.15.1、PyTorch 2.11.0、Matplotlib 3.10.0。本轮 `easy_picture` 实验另在 Python 3.12.4、PyTorch 2.8.0+cu128 和 NVIDIA RTX 4070 Laptop GPU 上完成并通过 CUDA FFT 检查。

## 目录结构

```text
gray_new/
├─ README.md
├─ doc/                     # 需求、算法说明和概念迁移文档
│  ├─ project-architecture.md
│  └─ concept_migration_pack/
├─ coding/
│  ├─ python/               # 优化、目标生成、权重计算和结果可视化脚本
│  └─ matlab/               # 相位到 CIF 掩模转换脚本
├─ visualization_tools/     # 可独立运行的可视化小工具
│  └─ order_visualizer/     # 级次选择可视化网站
├─ tests/                   # 固定前向和校准目标回归测试
├─ input/                   # 原始输入数据
│  ├─ grayscale_image.mat
│  ├─ thin_letter_reference.png
│  └─ geometric_network_reference.png
└─ output/                  # 仅存放可删除、可重新生成的运行结果
```

`input/` 与 `output/` 严格分离：用户提供的原图或 MAT 数据只放入 `input/`；目标 MAT、训练结果、指标和预览图只放入 `output/`。2026-08-31 已按用户要求清空旧 `output/`，因此下文早期实验路径仅作为历史指标记录，文件本身已不存在；当前磁盘只保留本轮几何线稿实验产物。

## 运行方式

### 本地级次选择可视化网站

在项目根目录启动静态服务器：

```powershell
python -m http.server 4173 --directory visualization_tools/order_visualizer
```

浏览器打开 `http://127.0.0.1:4173/`。图片仅在浏览器内读取，不上传到网络。默认值为 `channel=9`、`lambda=480 nm`、`P=2000 nm`、`m,n=-3…-1`、`theta=42.7517°`、`phi=45°`、`n_in=n_out=1`、`NA=1`，标称圆形 FoV 约 `42.20°`。channel 只能从 `4、9、16…400` 的完全平方数下拉列表选择。

网站只按每个已选级次的正方形方格裁剪图片；空气传播圆和 NA 圆作为物理范围参考，不裁剪图片。同时显示所有与空气圆相交的未选局部角谱方格，并在图和表中区分完整入圆、部分入圆和级次中心状态。公式、参数含义和边界见 [级次选择可视化网站](doc/order-visualizer-site.md)。

### 优化流程

在项目根目录运行主优化流程：

```powershell
python coding/python/order_decoupling_grayscale.py `
  --mat-file input/grayscale_image.mat `
  --output-dir output/run_001 `
  --device auto
```

快速验证：

```powershell
python -m compileall -q coding/python
python coding/python/order_decoupling_grayscale.py --mat-file input/grayscale_image.mat --output-dir output/smoke_test --epochs 1 --device cpu
```

固定物理前向回归验证：

```powershell
python tests/test_physical_forward.py
```

该测试使用独立的 NumPy 参考实现，对固定随机 `dx/dy` 的36个通道逐值比对，并检查 Parseval 能量关系、第一象限级次范围和共享相位配置。

生成三线四标签校准目标：

```powershell
python coding/python/create_three_line_target.py --output-dir output/three_line_calibration
```

工具生成 `36 × 500 × 500` 的 `bw_all` MAT 文件、统一曝光预览图和逐通道像素计数 CSV。三条水平线长度 200、宽度 5、中心行间隔 60，从上到下为 `1/3、2/3、1`。

从 `input/grayscale_image.mat` 的二值水果线稿生成填充三灰度目标：

```powershell
python coding/python/create_three_level_fruit_target.py `
  --output-dir output/fruit_three_level_filled
```

脚本先逐通道填充封闭轮廓，再按距离边界的远近生成外层 `1/3`、中层 `2/3`、核心 `1`，输出 MAT、预览图和像素统计。

### 6×6 满幅细线字母目标

用户提供的字母细线图已保存为 `input/thin_letter_reference.png`。运行：

```powershell
python coding/python/create_thin_letter_target.py
```

脚本先按有效内容范围切成6×6共36块，并将全部图块按行优先顺序映射到 `(1,1)…(6,6)`。每个图块直接重采样铺满500×500 channel，禁止居中粘贴或追加黑色保护区；黑色只表示原图中主体线条之间的真实背景。接近背景的浅装饰纹会被过滤，保留线条再量化为 `1/3、2/3、1` 三档。36通道目标位于 `output/thin_letter_6x6_target_36`。

对像素值已固定为 `0/85/170/255` 且含纯黑网格的输入，使用 `--level-assignment fixed --min-tile-line-pixels 30`。纯黑格不会进入优化；只含部分灰档的有效格保留原样，每档CV仅统计实际包含该档的channel。

最新几何线稿使用空间连续分档，避免细线的三个灰度只落在线宽抗锯齿边缘而视觉上接近全白：

```powershell
python coding/python/create_thin_letter_target.py `
  --input-file input/geometric_network_reference.png `
  --output-dir output/geometric_network_target_36 `
  --background-delta 15 `
  --min-line-intensity 30 `
  --level-assignment spatial
```

`spatial` 会把每个满幅图块中的有效线条按空间位置连续划分为深灰、中灰、亮灰三组；`source` 则保留按原图像素亮度量化的对照方式。两种方式都不会增加通道边缘保护区，也不会对优化结果做逐通道增益。

本轮约 1000 次验证命令为：

```powershell
python coding/python/order_decoupling_grayscale.py `
  --mat-file output/thin_letter_6x6_target/thin_letter_6x6_target.mat `
  --output-dir output/thin_letter_main_strokes_1000 `
  --epochs 1000 `
  --lr 1e-3 `
  --device auto
```

该验证仅使用主图案损失，没有逐 channel 亮度缩放，也没有提前加入 CV、背景或线内均匀辅助项。

阶段 D 短轮实验可从已有结果 warm-start，并固定阶段 C 的公共尺度 `Q`：

```powershell
python coding/python/order_decoupling_grayscale.py `
  --mat-file output/three_line_calibration/three_line_target.mat `
  --output-dir output/stage_d_candidate_007 `
  --epochs 1500 `
  --lr 5e-4 `
  --device auto `
  --initial-results output/baseline_four_level_lines/optimized_results.npz `
  --reference-q 29.23249 `
  --level-weight 0.2 `
  --cross-level-weight 0.1 `
  --gap-weight 0.02 `
  --line-uniformity-weight 0.02 `
  --visibility-weight 0.05 `
  --worst-level-weight 0.1 `
  --background-uniformity-weight 0.2 `
  --background-row-uniformity-weight 3.0
```

主流程会额外输出 `grayscale_level_metrics.csv`，包含三档有效响应、比例、单调性、背景方差和背景逐行方差。稀疏三灰度目标默认使用 `--image-loss-mode energy`，直接匹配目标与重建的总能量分布，不再使用容易受单个亮斑支配的最大值归一化。用 `--channel-count N` 可以只优化前 N 个通道，用于检查共享相位变量的多通道可行性。

每次优化结束还会生成 `evaluation_channel_metrics.csv` 和 `evaluation_summary.csv`，并在终端打印五项评价。指标定义和读法见 [优化结果评价指标](doc/evaluation-metrics.md)。

水果目标从随机相位起跑时，先保持灰度、线内均匀性和背景辅助损失为默认的 `0`。旧的 `balanced` 加多项辅助损失组合会在早期压过图案主损失，使三档比例数值看似正确但整体亮度塌缩。基础图案收敛后如需改善跨通道亮度或背景，再从已有结果热启动并逐项增加约束。

背景亮度带实验不针对横向纹理，而是对所有背景像素统一计算 `I / 全平面均值` 的越界惩罚。只限制亮斑时使用 `--background-band-lower 0 --background-band-upper 1.0`；同时限制过亮和过暗区域时，例如使用 `--background-band-lower 0.6 --background-band-upper 1.0`。两种方案的权重由 `--background-band-weight` 控制。

背景局部集中实验使用 `--background-cluster-weight`、`--background-cluster-kernel` 和 `--background-cluster-upper`。它先计算每个背景像素邻域内的背景平均亮度，再惩罚超过局部上限的邻域；9×9 主要观察小亮斑/小亮块，21×21 主要观察较宽的成块和扩散纹。

本轮已完成两组短实验：`output/stage_d_background_band_upper`（上限方案）和 `output/stage_d_background_band_two_sided`（上下限方案）。两组均保持 23/23 通道三档响应单调，但统一曝光图中仍能看到线条附近的残余扩散纹，因此暂不视为通过背景验收。

随后完成两组局部集中短实验：`output/stage_d_background_cluster_9x9` 和 `output/stage_d_background_cluster_21x21`。9×9 结果的背景方差约为 `0.5214`，21×21 约为 `0.5280`；两者仍保留三档结构，但尚未消除可见残余扩散纹。

在此基础上又完成 9×9 加强实验 `output/stage_d_background_cluster_9x9_stronger`：局部集中权重为 `2.0`、局部上限为 `0.9`，背景方差降至约 `0.5064`，但统一曝光图仍残留扩散纹，尚不能作为最终方案。

输出目录必须是尚不存在的新目录。完整优化默认 30000 轮，运行时间和显存/内存占用明显高于快速验证。

辅助脚本：

```powershell
python coding/python/calculate_weight.py
python coding/python/show_save_img.py
python coding/python/reproduce_li_zhongyang_fov_examples.py
```

优化前检查一张四灰度源图是否适合按6×6切成36个channel：

```powershell
python coding/python/evaluate_source_image_channels.py `
  --input-file input/block_25.png `
  --output-dir output/block_25_source_evaluation
```

脚本先在内存中按最近值量化为严格的`0/85/170/255`。逐channel的
`channel_target_mean`把黑底计入，是主要的总体目标亮度；
`foreground_gray_mean`只统计原三档非零灰度，用于区分灰阶配比偏差与线条覆盖率偏差。
汇总文件报告这两项及线条覆盖率、边界复杂度的跨channel CV，并统计每个channel
是否包含完整三档灰度。需要保存量化图时可额外传入`--quantize-output <path>`。

生成7×7圆形四灰度目标（每块5000×5000缩小到500×500，总图3500×3500）：

```powershell
python coding/python/create_circular_7x7_target.py
```

该脚本逐块读取以控制内存，使用最近邻缩放并验证输出仍严格只有`0/85/170/255`。圆外区域置零，纯黑角块不写入`bw_all`；优化时使用`--order-grid-size 7 --channel-count 45`，物理级次按MAT内的`grid_positions`映射，而不是简单取前45个级次。

主优化器支持用 `--order-m-start` 和 `--order-n-start` 平移整个连续级次方阵；两个参数默认均为1，因此旧命令和旧结果不变。圆形45通道在 `|m|,|n|≤9` 的连续7×7候选中，复杂度加权的推荐起点为 `(-9,3)`，即 `m=-9…-3、n=3…9`。该方案仍保持连续网格，不进行任意离散选阶；相对当前 `(1…7,1…7)`，有效通道内的共线倍数对由20降至16，加法组合关系由172降至29，复杂度最高10个图块的平均关系负担由9.45降至1.40。

推荐连续级次的100轮公平对照位于 `output/circular_7x7_orders_m-9_-3_n3_9_main_100`。它与原100轮使用相同目标、损失、学习率和seed42，仅改变级次起点：左上8个复杂通道的平均结构相似度由0.279提高到0.302，但全局结构均值由0.314降至0.299；三档跨通道CV由 `0.971/0.983/0.985` 改善为 `0.916/0.887/0.887`。因此避开部分倍频关系改善了左上复杂区域和初期一致性，但没有让全局结构在100轮内立即提升。共同曝光对比位于 `output/circular_7x7_order_layout_100_comparison`。最高9阶在真实器件中的传播、数值孔径和采样可行性尚未验证。

连续级次的物理传播条件可用 [衍射级次物理可行性验证](doc/diffraction-order-validation.md) 检查。此前验证器只按一个共同入射角判断传播，并把改变周期造成的级次间距变化误当成了完整的“移到中心”方案；现已纠正为空气锥在实验室坐标中固定、入射横向波矢使级次点阵平移。工具现在明确区分：固定入射时多个级次位于不同方向并扩展大视场；逐级次居中时则为每个目标级次分别反算一组入射极角和方位角。它还单独报告 `(0,0)` 不可控、正负级次共轭和同方向整数谐波关系，不再把“可传播”直接称为“可解耦”。

李仲阳团队相关论文的逐项核对记录见 [衍射级次解耦与大视场论文阅读记录](doc/li-zhongyang-large-fov-reading-notes.md)。补充材料 Figure S2/S9 证明，大视场图案应理解为连续级次方格所承载的局部角谱拼接后，再被圆形传播域裁切；圆周相交的外围方格应保留圆内部分，而不是只依据级次中心取舍。近180°示例同时使用3×3正负共轭级次、零级以及PB旋转相位。当前方形纳米砖和 `dx/dy` 位移前向不含PB自由度，因此只能先实现局部角谱圆形裁切验证，不能在结构完全冻结时宣称复现任意图案的近180°独立编码。

重新读取论文公开补充材料后，已撤回“级次必须以 `(0,0)` 为中心”以及“仅由最远级次中心乘二估算视场”的错误假设。论文23通道使用偏置的单侧级次集合：`m=-3,-2,-1` 与 `n=-3…3` 的21项，再加 `(0,1)`、`(0,2)`；约46°的+x方向斜入射把该集合整体平移进空气锥。补充材料还说明最终大视场由相邻级次承载的全息角谱区域级联形成，不等于离散级次中心的跨度。近180°扩展方案进一步加入PB相位，用纳米结构旋转自由度独立编码共轭级次，并让零级也能由PB相位承载图案；当前只优化 `dx/dy` 的代码尚不具备这一自由度。验证器已加入 `--order-preset paper-23` 和 `--conjugate-encoding detour-only|detour-pb`，能够区分论文23级次布局与纯迂回相位/混合PB编码边界。

按补充材料参数复算的验证位于 `output/diffraction_validation_paper23_488nm_2000nm_46deg`：23/23级次均可传播和收集，计算得到 `(-3,3)`、`(-2,0)`、`(-1,0)` 的极角分别为 `47.064°、13.376°、28.381°`，与Table S3的 `47.06°、13.38°、28.38°` 一致，说明当前光栅方程和入射角约定已与论文对齐。

针对7×7大圆形目标，新的连续级次推荐为 `m=-7…-1、n=-3…3`。它保留连续7×7映射，同时避开零级和正负共轭配对；以 `lambda=488 nm`、空气和 `NA=1` 验证时，`P=2200 nm、theta_i=62.5325°、phi_i=0°` 可将级次块中心平移到空气锥中心，45/45个非黑通道全部传播和收集，最差空气锥余量为0.2002，最外有效级次极角为53.11°。对照的2000 nm方案可扩展到61.61°但需要77.42°近掠入射，2400 nm方案余量提高到0.2669但最外极角缩小为47.15°，因此当前推荐2200 nm折中。验证结果位于 `output/circular_order_validation_m-7_-1_n-3_3_P2200`，详细假设和命令见 [衍射级次物理可行性验证](doc/diffraction-order-validation.md)。该结果只验证级次中心的传播和收集，不代表已经完成45通道图像解耦、器件效率或完整连续视场验证。

2026-09-05用户进一步锁定“器件物理结构不得做任何改变”，因此上述2000/2200/2400 nm结果只保留为理论周期对照，不再作为当前器件的实施推荐。若真实结构参数为 `lambda=480 nm、P_x=P_y=850 nm`，级次中心间距约0.5647，连续7×7中心阵列跨度约3.388，超过空气锥直径2；仅改变连续级次编号和入射角无法让45个中心全部传播。固定结构的新目标是保留连续正方形级次全集，以自由空间空气锥裁成圆形有效视场，并把与圆边界相交的级次角谱区域纳入；初步中心搜索最多有12个级次中心入圆，按方格化局部角谱估计约16个级次区域与圆相交。圆外仍可传播的部分可作为隐藏/保护区域，完全倏逝的级次不能作为可显示远场通道。正式实现前还需确认850 nm究竟是有效衍射周期还是仅为纳米单元节距。

用户随后澄清，目标只针对当前圆形山水图，不要求支持任意圆图或所有级次独立；复杂图块需要优先避开零级、共轭和强谐波关系，简单图块可以承担相关级次，约100°以上即有价值。按论文的局部角谱拼接模型，固定 `lambda=480 nm、P=850 nm` 时，3×3完整圆的几何视场约115.79°；4×4可触及空气锥边界但会裁掉约11%的原图外围半径；6×6/7×7会丢失过多外围内容。因此当前建议先公平比较3×3高保真方案与4×4大视场方案。4×4初步候选为 `m=0…3、n=-2…1`、入射约 `theta_i=63.24°、phi_i=161.57°`，可把零级和唯一共轭对放到较简单的上边缘图块；该推荐尚未经过局部角谱圆裁切实现和优化短跑验证。

3×3的当前优先候选为连续级次 `m=0…2、n=-2…0`，共同入射约 `theta_i=53.00°、phi_i=135.00°`。该选区没有正负共轭对，只需把不可控零级分配给最简单的边缘图块；它比4×4更有希望在保留完整圆图的同时取得超过100°的实际视场和较好的结构质量。

6×6/7×7并非绝对不可用。`coding/python/validate_circular_grid_visibility.py` 提供两种条件性验证：`native_fill` 保持原图铺满方阵并量化空气锥裁掉的图像细节；`fit_mode` 将完整圆图缩进可传播圆内，把外围级次作为黑色/保护通道。该验证用于判断较大方阵是否值得进入优化短跑，不把几何可见性等同于图像解耦成功。

用户允许目标圆适度超出严格空气锥后，已增加`--overscan-factor`参数并完成`1.2×`过扫描短跑。6×6使用23个非黑、非零级通道，100轮结构余弦均值为0.253、三档CV约1.48；7×7使用20个通道，结构余弦均值为0.273、三档CV约1.00。两组目前只出现弱圆框，尚未恢复山水细节，因此结论是“可以继续验证，但不能凭100轮认定成功”。几何预览和目标位于`output/circular_grid_6x6_overscan120`与`output/circular_grid_7x7_overscan120`，优化结果位于`output/circular_overscan120_6x6_main_100`与`output/circular_overscan120_7x7_main_100`。

当前阶段只确定物理级次布局，不依据短轮图像效果选阶。用户已把“`(0,0)`绝对禁止、有效图片区域必须完整6×6”设为硬约束，因此此前包含零级的`m=-1…4、n=-3…2`候选作废。

按论文23-channel的单侧思路，新诊断集合为连续6×6 `m=-6…-1、n=-3…2`，零级和正负共轭对均为0。论文使用`P=2000 nm、lambda=488 nm`，级次间距0.244，约46°斜入射后23/23级次传播；当前`P=850 nm、lambda=480 nm`间距为0.5647，对所有允许入射方向穷举后，无零级6×6最多只能传播10/36个中心，无法形成完整有效6×6。使用`P=2000 nm`作纯理论对照时，相同无零级6×6在约`theta_i=58.05°、phi_i=8.13°`下可实现36/36传播。结果分别位于`output/paper23_order_layout_recheck`、`output/diffraction_orders_6x6_nozero_P850_max10`和`output/diffraction_orders_6x6_nozero_P2000_theory`。在确认850 nm是否只是超胞单元节距以前，不进入正式训练，也不把2000 nm对照当作可实施修改。

入射角约定为：`theta_i`从表面法线`+z`量起，`phi_i`为横向投影从`+x`朝`+y`的方位角。`P=2000 nm`理论6×6的圆形图案方向余弦半径为0.72，对应全视场约92.11°；方阵中心对角线的最大射线夹角约116.10°，两者不可混用。新增`coding/python/analyze_image_order_multiples.py`，直接按约化后的`(m,n)`方向统计整数倍耦合。当前山水图最复杂12块中，6块无倍数伙伴；发生倍频的级次为`(-4,-2)`、`(-4,2)`、`(-3,-3)`、`(-2,-2)`、`(-2,-1)`、`(-2,0)`。对可行无零级6×6方向搜索后，当前`m=-6…-1、n=-3…2`仍具有最低的复杂度加权倍频负担，分析图和CSV位于`output/circular_6x6_nozero_multiple_analysis`。

`n=0`但`m≠0`的级次可由`dx`编码，并非零级；但`(-1,0)…(-6,0)`全部属于同一轴向整数倍组，只依赖一个位移自由度，对互不相同的复杂图块风险很高。当前映射中的`(-2,0)`图块复杂度约0.90，因此必须通过隔离实验判断是否保留。用于开启低上下文新任务的两个完整提示词见[6×6级次新对话提示词](doc/new-thread-prompts-6x6-orders.md)：第一个实跑当前组合并单独评价`n=0`列，第二个只继续搜索连续无零级6×6组合。

按现有45个非黑图块原位映射复算时，45/45均可传播，但包含1个不可控零级，其余44个组成22对正负共轭，因此只有22个“一对共轭取一个”的候选；进一步排除整数谐波后为16个保守候选。连续、居中、45个任意独立图像这三个要求在当前 `m*dx+n*dy` 模型下不能同时成立。

7×7结果可用通用场景对比脚本重排：

```powershell
python coding/python/compare_full_scene_results.py `
  --input-file output/circular_7x7_target_49/circular_scene_3500.png `
  --output-dir output/circular_7x7_main_100_comparison `
  --result-dirs output/circular_7x7_main_100 `
  --grid-size 7 `
  --illumination-factor 2
```

在 `output/circular_7x7_main_100` 的100轮纯主损失结果上继续运行2400轮，可得到累计约2500轮的结果（45个有效通道，4个纯黑角通道不参与优化）：

```powershell
python coding/python/order_decoupling_grayscale.py `
  --mat-file output/circular_7x7_target_49/circular_7x7_target.mat `
  --epochs 2400 `
  --lr 5e-4 `
  --seed 42 `
  --channel-count 45 `
  --order-grid-size 7 `
  --image-loss-mode energy `
  --device cuda `
  --output-dir output/circular_7x7_main_total_2500 `
  --log-interval 100 `
  --initial-results output/circular_7x7_main_100/optimized_results.npz
```

本次实测耗时约1599秒。累计2500轮后，主损失从续跑起点约96.49降至73.45，结构余弦相似度均值从100轮的0.314提升到0.483，三档比例从约`0.342:0.672:1`变为`0.361:0.685:1`。三档跨通道CV仅从`0.971/0.983/0.985`降至`0.861/0.840/0.839`，说明纯主损失增加轮数能明显恢复结构，但不能单独解决跨通道亮度不一致。结果保存在`output/circular_7x7_main_total_2500`，100轮与累计2500轮的共享2×曝光对比保存在`output/circular_7x7_main_100_vs_total_2500_comparison`。

全局入射光强测试：

```powershell
python coding/python/simulate_global_illumination.py `
  --results-file output/geometric_network_36_brightness_q2_strong_stage2_500/optimized_results.npz `
  --output-dir output/global_illumination_test_1x_2x_4x `
  --factors 1 2 4
```

该测试只对全部36个通道共同乘以一个光强倍率，不重新优化相位，也不允许逐channel增益。固定曝光图用于观察整体变亮；每种倍率分别归一化的图会完全相同，不能用于判断绝对亮度。

生成多个36通道结果的完整场景左右对比：

```powershell
python coding/python/compare_full_scene_results.py `
  --input-file input/block_test.png `
  --output-dir output/block_test_full_scene_comparisons_2x `
  --illumination-factor 2 `
  --result-dirs output/block_test_36_main_1000 output/block_test_joint_stage1_500
```

工具使用所有结果共同的单一显示尺度，左侧为原图，右侧为按6×6顺序重排后的优化结果；不会逐通道或逐结果归一化。每个阶段单独输出一张对比图，同时生成 `all_stages_original_vs_optimized.png` 总览。

`show_save_img.py` 和 `coding/matlab/mask_generate.m` 保留了原有的本机绝对路径/固定目录配置，使用前需要在脚本内按实际结果位置修改。当前仍未改写这些路径。

## 核心架构与流程

主流程是：输入目标 → 初始化共享 `dx/dy` → 固定物理前向重建各通道 → 计算损失并用 Adam 优化 → 重建与计算指标 → 写入 NPZ、CSV 和 PNG。

不可变物理映射为 `Φc = mc·dx + nc·dy`、`Uc = exp(iΦc)`、`Ic = |fftshift(FFT2(Uc))|²`。详细模块职责、数据流和输出格式见 [项目架构说明](doc/project-architecture.md)，当前目标见 [四级灰度需求](doc/grayscale-requirements.md)，执行步骤见 [四级灰度实施计划](doc/grayscale-implementation-plan.md)，理论与迁移约束见 [概念迁移包](doc/concept_migration_pack/README.md)。

## 配置与外部依赖

主脚本通过命令行参数配置 MAT 文件、轮数、学习率、设备、输出目录、日志间隔、灰度一致性损失、背景均匀性损失和背景亮度带上下限。`--desired-gray-ratios LOW MID` 可设置最低档和中间档相对最高档的目标响应，默认仍为 `1/3 2/3`，并要求 `0 < LOW < MID < 1`。没有环境变量、网络服务、数据库或认证依赖。

MAT输入必须包含 `bw_all`，默认6×6模式允许1–36个通道，7×7模式允许1–49个通道；标签只能是 `0/1` 或 `0、1/3、2/3、1`。MATLAB脚本另依赖优化流程导出的 `phdx.csv` 和 `phdy.csv`；当前Python主脚本只在NPZ中保存这两个数组，并不导出对应CSV。

## 已知限制与风险

- 本地网站是二维光栅方程与图片—级次映射的交互可视化，不执行共享相位优化，也不代表器件效率、串扰或 FDTD/RCWA 结果。当前页面使用单一等向周期 `P_x=P_y=P`；浏览器不能解码的 TIFF 等格式需先转成 PNG、JPG 或 WEBP。
- 当前默认级次为第一象限 `(1,1)…(6,6)` 共36项，基础损失权重统一为1；旧23通道MAT仍可显式使用 `--channel-count 23`。
- 7×7模式可通过`--order-grid-size 7`启用，最多49个连续级次；圆形目标当前有45个非空通道。级次起点默认仍为正级次 `(1,1)`，也可显式平移到其他象限。连续方阵可能包含整数倍谐波耦合，是否优于6×6必须以实际结构、CV和物理传播验证共同判断。
- 级次验证器只做光栅方程层面的传播、NA收集、逐级次居中入射反算和严格相位关系筛选；“运动学可行”不等于“已独立解耦”。真实效率、串扰、偏振、材料损耗和结构共振仍需联合优化以及RCWA/FDTD。仓库中的850 nm可能是纳米单元间距而非有效衍射周期，尚未确认。
- 6×6正级次没有正负共轭或坐标轴级次，但含有 `(1,1)/(2,2)…` 等整数倍谐波关系，因此36项不是36个完全独立方向。
- 主脚本以逐通道循环执行 2D FFT，完整训练计算量较大。
- 辅助可视化和 MATLAB 脚本含本机路径，尚未参数化。
- Python 输出与 MATLAB 输入格式目前没有直接衔接：MATLAB 需要相位 CSV，主流程只生成 NPZ。
- `mask_generate.m` 原文件存在中文注释编码显示异常；为保持源码不变，本次未转换编码。
- 阶段 C 已完成一次 30000 轮固定比例基线；阶段 D 已完成多组短轮探索，并新增全背景亮度带与局部集中方案。背景残余扩散纹仍未解决；阶段 E 的 9×9 候选 5000 轮正式短跑（seed42）已完成，尚未进入第二随机种子复核。这些结果不等同于 FDTD/器件验证。
- 阶段 G 已从 23 张二值水果线稿重新生成填充三灰度目标，填充后前景占比约 `1.98%`。已修复二值输入误计算空灰度档导致的 NaN，并加入 `--channel-count` 通道缩减验证。进一步确认最大值归一化 MSE 与多项线条辅助损失会导致亮度塌缩，现已改用总能量归一化的灰度分布损失。

## 当前状态与下一步

项目已完成目录归类、架构梳理和主流程最小运行验证。当前处于 `experiment/four-level-lines` 分支的阶段 G。按用户指定的水果灰度轮廓线目标 `output/fruit_three_level/fruit_three_level_target.mat` 完成了 23 通道、1000 轮验证，结果位于 `output/verify_fruit_three_level_lines_ch23_1000`：23/23 通道三档均单调，平均比例约为 `0.351:0.683:1`，所有水果轮廓线在统一曝光图中可辨认。但三个灰度档的跨通道 CV 均约为 `0.53`，最高档响应范围为 `2.52–14.68`，尚未达到通道亮度一致。

下一步采用两阶段热启动：从上述结果继续优化，固定公共最高档尺度 `Q=5.834266`（本轮最高档响应均值），只启用低权重的 `level` 与 `cross-level` 约束，把每个通道三档响应拉向共同的 `Q/3、2Q/3、Q`。暂不同时加入线内均匀性和背景约束，避免再次压过图案主损失；候选结果必须同时检查跨通道 CV、最弱通道、前景效率和统一曝光轮廓。

首个一致性热启动候选 `output/verify_channel_consistency_1000` 已完成1000轮。三个灰度档的跨通道 CV 从约 `0.53` 降至 `0.31–0.33`；最高档最小值从 `2.52` 提升到 `4.63`、最大值从 `14.68` 降至 `13.27`，最大/最小比从约 `5.82` 降至 `2.87`。23/23 通道保持单调，平均前景效率由 `4.46%` 提升到 `5.87%`。方向有效，但当前低权重尚未达到最终通道亮度一致。

按新增四项评价复算该候选：结构余弦相似度均值/最差为 `0.661/0.505`，前景覆盖率均值/最差为 `88.3%/71.1%`；23/23 通道灰度单调，比例 RMSE 均值/最差为 `0.022/0.033`；三档跨通道 CV 为 `0.322/0.327/0.310`；背景像素 CV 约 `1.00`、P95/均值约 `3.00`、逐行 CV 约 `0.048`。因此当前结论是轮廓大体形成、通道内灰度分级良好、跨通道一致性仍不足、背景存在明显细粒度散斑但无强烈行方向起伏。

已进入四目标联合损失阶段。改动严格限定在损失函数：新增逐目标像素最低响应项 `structure-completeness`，并将线内均匀性改为按各灰度线自身均值归一化；固定物理前向、目标、通道共享关系和显示方式均未改变。

四目标联合优化完成三个递进候选，当前综合最优为 `output/joint_loss_candidate_3_1000`。最终结构相似度均值/最差为 `0.761/0.740`，前景覆盖率均值/最差为 `98.2%/94.2%`；23/23 通道灰度单调，比例 RMSE 均值/最差为 `0.0092/0.0133`，三档均值比例约为 `0.347:0.675:1`；三档跨通道 CV 为 `0.138/0.139/0.135`；背景像素 CV 为 `0.945`、P95/均值为 `2.905`、逐行 CV 为 `0.052`。与联合优化前相比，结构、灰度比例、同档一致性和像素级背景均有改善，逐行背景CV略有增加但仍处于较低水平。

用户随后将同灰度跨通道CV明确设为最高优先级。CV优先热启动结果位于 `output/cv_priority_candidate_1000`：三档跨通道CV进一步降到 `0.026/0.043/0.050`；结构相似度均值/最差为 `0.771/0.725`，前景覆盖率为 `98.7%/95.8%`；23/23通道单调，比例RMSE均值为 `0.0077`；背景像素CV为 `0.949`，未出现明显恶化。该结果取代第三联合候选，成为当前推荐结果。

新增满幅细线字母验证 `output/thin_letter_main_strokes_1000`。目标来自 6×6 网格中的 23 个高对比主体线条块，未添加保护区。1000 轮纯主图案损失后，结构相似度均值/最差为 `0.530/0.392`，前景覆盖率均值/最差为 `34.1%/15.3%`；23/23 通道三档单调，比例 RMSE 均值/最差为 `0.015/0.024`；三档跨通道 CV 为 `0.277/0.271/0.276`；背景像素 CV 为 `1.000`、P95/均值为 `2.995`、逐行 CV 为 `0.046`。统一曝光下各通道主体轮廓可辨，但细线偏暗且部分低灰度笔画不完整，因此这是可行性结果而非最终高质量候选；后续若继续，应从该相位热启动并在结构不回退的前提下再加入跨通道一致性损失。

针对细线灰度不明显与同档不一致，已验证“单边抬亮 + 一致性收尾”的损失顺序。对称 `level` 损失会同时压低过亮通道，不适合作为可见性主项；最终方案使用 `reference-q=4`，以 `worst-level` 作为只惩罚低于 `Q×[1/3,2/3,1]` 的亮度下限，再用较强 `cross-level` 收紧同档跨通道差异。最终候选位于 `output/thin_letter_gray_final_candidate_300`：结构相似度均值/最差 `0.547/0.468`，覆盖率均值/最差 `37.1%/24.8%`，23/23 通道单调，比例 RMSE 均值 `0.0218`；三档 CV 降至 `0.035/0.079/0.102`，最高档均值/最小值从原始 `2.21/1.25` 提高到 `2.45/1.68`，背景指标基本不变。优化前后图表由 `coding/python/plot_grayscale_comparison.py` 生成，结果为 `output/thin_letter_gray_final_candidate_300/grayscale_loss_comparison.png`。

按用户确认新增36级次方案：`PAIR_MAT={(m,n)|m,n∈{1,…,6}}`，不含零轴和负级次，36项基础权重统一为1。目标位于 `output/thin_letter_6x6_target_36`，1000轮随机相位结构基线位于 `output/thin_letter_36channels_main_1000`。结果结构相似度均值/最差为 `0.491/0.155`，覆盖率均值/最差为 `34.0%/6.3%`；36/36通道三档单调，比例RMSE均值为 `0.0189`；三档CV为 `0.567/0.570/0.572`。最差结果集中在 `(1,1)、(2,1)、(1,2)、(2,2)`及同方向谐波附近，证明仅排除正负共轭不足以保证级次独立；该配置已实现且可运行，但当前36通道一致性明显弱于原23通道候选。

当前不是 Keil 工程，不涉及标准空工程选择。

### 2026-09-05 当前结果：蝴蝶图连续无零级4×4级次分析

新增`coding/python/analyze_butterfly_4x4_orders.py`，对`input/butterfly.png`按4×4切分，以亮线密度和边缘总变差等权计算复杂度，枚举连续无零级方阵及8种整体旋转/镜像。推荐`m=-2…1、n=1…4`并将整个图块映射逆时针旋转90°。固定`lambda=480 nm、P=850 nm`时，共同入射方向为`theta_i=63.236°、phi_i=-71.555°`，10/16个级次中心传播，12/16个局部角谱方格与空气锥相交；最高复杂前4块的中心全部传播。

该方案零级和共轭对均为0，但包含`(0,1)…(0,4)`四成员轴向组；蝴蝶中央复杂区域无法在保持10/16传播的同时完全避开该组。推荐实际4×4角谱方格约51.66%面积进入空气锥，复杂度加权可见比例约60.89%。名义圆形FoV裁切上限为180°，但不是完整圆形180°图像覆盖。`P=2000 nm`理论对照下相同16级次以`theta_i=37.726°、phi_i=-78.690°`实现16/16传播，圆形FoV约57.37°。逐级次方向余弦、出射角、局部单元边界、空气锥交叠率和倍数组见[蝴蝶图连续无零级4×4级次与空间角分析](doc/butterfly-4x4-order-analysis.md)。本阶段没有执行图像优化。

进一步扫描850–2400 nm后确认：若允许改变光栅方程中的有效周期，2000 nm下可以改用完全无轴的连续方阵`m,n=1…4`。共同入射`theta_i=58.05194°、phi_i=-135°`时16/16中心传播，完整4×4局部角谱方格也位于空气圆内，圆形FoV为57.37080°；最复杂4块中2块无倍数伙伴，另2块位于4成员对角倍数组，但没有复杂块落入轴向组。周期增大不是无条件改善：1400 nm配合原跨轴方阵可保留约86.58°圆形FoV，2000 nm则以更小FoV换取无轴选阶、较温和入射角和更大传播余量。详细结果见[蝴蝶图4×4有效周期扫描](doc/butterfly-4x4-period-sweep.md)。若850 nm是冻结的真实有效衍射周期，2000 nm仍只能作为理论对照。

用户随后正式指定`lambda=480 nm、P_x=P_y=2000 nm、n_in=n_out=1、NA=1`和连续`m,n=1…4`进行蝴蝶图验证。整幅图统一`rot180`后按4×4切分，固定保留`0/85/170/255`，没有单块交换。物理复核确认共同入射`theta_i=58.05194°、phi_i=-135°`下16/16级次中心传播并收集，最小空气锥余量为`0.4909`。

100轮纯主损失只形成触角和弱局部结构；从该结果续跑2400轮后，累计2500轮候选`output/butterfly_4x4_m1_4_n1_4_main_total_2500`已形成完整可辨蝴蝶。结构相似度mean/min从`0.294/0.165`提高到`0.663/0.356`，覆盖率从`0.066/0.051`提高到`0.424/0.084`，16/16通道灰度单调，三档比例为`0.348:0.675:1`。三档跨通道CV仍为`1.170/1.179/1.183`，说明下一阶段应处理通道亮度一致性。100轮与2500轮共用尺度的统一2×曝光对比位于`output/butterfly_4x4_main_100_vs_total_2500_full_scene_2x`，完整命令和重点图块指标见[蝴蝶图连续4×4级次优化记录](doc/butterfly-4x4-optimization.md)。

早期评价实现曾使用`CNR=(mu_fg-mu_bg)/sigma_bg`及旧的`SNR_dB=20log10(|CNR|)`。按该历史口径，2500轮蝴蝶的CNR mean/min=`3.331/0.404`，旧SNR mean/min/max=`5.23/-7.87/22.03 dB`；这些数值保留用于复现实验，但当前SNR已改为论文口径，不能直接横比。结果位于`output/butterfly_4x4_m1_4_n1_4_main_total_2500_snr_evaluation`。公式与版本边界见[优化结果评价指标](doc/evaluation-metrics.md)。

### 2026-09-05 当前结果：falcons中心对称线稿5×5/6×6选阶

针对`input/falcons.png`在`lambda=480 nm、P=2000 nm`下比较连续无零级5×5与6×6。推荐先用`m=-4…0、n=1…5`的5×5原始映射：共同入射`theta_i=59.92067°、phi_i=-56.30993°`时25/25中心传播，完整局部角谱方格余量为`0.15147`，标称圆形FoV为`73.73980°`。最复杂中心图块映射到无倍频的`(-2,3)`；5个`m=0`轴向级次只承接复杂度排名`13、14、23、24、25`的底部羽毛，最高复杂前12块均不在轴向组。

6×6对照`m=-2…3、n=1…6`可实现36/36中心传播和`92.10896°`标称FoV，但完整角谱方阵四角半径为`1.01823`，约`0.066%`面积被空气圆裁切；复杂度排名9–11的中心图块还会进入6成员轴向组。综合中心细节保护、传播余量与共享相位优化负担，5×5更适合作为第一轮。完整分析见[falcons中心对称线稿的5×5/6×6级次选择](doc/falcons-order-grid-selection.md)。初始选阶阶段未执行图像优化。

用户确认5×5后，已用`coding/python/create_falcons_5x5_target.py`按identity整幅等分并最近邻缩放为25个500×500目标，严格保留`0/85/170/255 → 0/1/3/2/3/1`。100轮纯主结构短基线位于`output/falcons_5x5_m-4_0_n1_5_main_100`：输出`pairMat`逐项等于`m=-4…0、n=1…5`且不含`(0,0)`；结构相似度mean/min为`0.2778/0.2549`，覆盖率mean/min为`0.0557/0.0502`，三档CV为`0.604/0.633/0.614`，CNR mean/min为`0.100/0.0129`，SNR mean/min为`-22.10/-37.82 dB`。统一2×曝光下仍以散斑为主，猫头鹰基本轮廓尚未形成，因此遵照“暂不长轮优化”要求，没有直接追加到2500轮。完整命令和判断已写入[falcons中心对称线稿的5×5/6×6级次选择](doc/falcons-order-grid-selection.md)。

用户随后明确要求从100轮结果再追加2500轮，累计2600轮纯主损失结果位于`output/falcons_5x5_m-4_0_n1_5_main_total_2600`。结构相似度mean/min提高到`0.5598/0.2959`，覆盖率提高到`0.2763/0.0630`，25/25通道灰度单调，比例RMSE mean/max为`0.0184/0.0417`，三档CV降至`0.449/0.444/0.439`；CNR mean/min提高到`1.346/0.194`，SNR mean/min/max为`1.09/-14.25/8.09 dB`，16/25通道高于0 dB。统一2×曝光下完整猫头鹰、双眼、喙、脸盘、额头和外围羽毛均已可辨，但背景散斑和通道亮度差异仍明显。100轮与2600轮共尺度对比位于`output/falcons_5x5_main_100_vs_total_2600_full_scene_2x`。

针对累计2600轮结果左下角较弱的问题，已确认左下2×2对应通道16、17、21、22，即`(-1,1)、(-1,2)、(0,1)、(0,2)`；四者都属于整数倍共线组，平均结构/覆盖率/SNR仅为`0.345/0.083/-9.33 dB`，而其余21通道为`0.601/0.313/3.07 dB`。主优化器因此新增显式的`--priority-channels`和`--priority-channel-weight`，只提高指定通道原有主结构损失的权重，不改变共享相位前向、dx/dy编码、级次映射或曝光。经过左下角优先、整体亮度CV和三档CV分阶段热启动，最终背景约束候选`output/falcons_5x5_snr_strong_stage8_300`达到：整体channel亮度CV `0.0229`，三档CV `0.0197/0.0487/0.0794`，结构mean/min `0.4823/0.4153`，覆盖率mean/min `0.1438/0.1122`。左下四块结构/覆盖率/SNR均值提高到`0.449/0.143/-2.24 dB`。相对纯主2600轮，平均结构和覆盖率有所下降，但最差结构、最差覆盖率、亮度一致性和左下角完整性明显提高。

轻度与强化背景方差约束只能把SNR mean/min从CV达标节点的`-1.40/-4.01 dB`小幅改善到`-1.35/-3.55 dB`，背景像素CV仍为`0.994`。因此当前结果证明可以在固定共享相位模型下同时实现左下角改善及全局/三档CV低于0.1，但尚未解决细粒度散斑；继续简单增加轮数或继续堆背景方差权重不太可能带来同量级提升。阶段命令、逐通道数据和取舍见[falcons中心对称线稿的5×5/6×6级次选择](doc/falcons-order-grid-selection.md)，四阶段统一2×曝光对比位于`output/falcons_5x5_left_bottom_cv_snr_comparison`。

随后按用户要求直接优化前景/背景分离。主优化器新增`--foreground-background-cnr-weight`，使用`softplus(1-CNR)`作为有界、尺度不敏感的训练项；它不能被公共曝光倍率伪造。经过300/500轮递增权重扫描并联合背景方差、背景亮点上限及9×9局部亮团约束，新推荐结果为`output/falcons_5x5_background_suppression_stage15_500`。相对前一候选，背景像素CV由`0.9937`降至`0.9162`，背景P95/均值由`2.984`降至`2.840`，SNR mean/min由`-1.35/-3.55 dB`提高到`0.22/-3.08 dB`，正SNR通道由0/25提高到18/25；前景平均原始亮度提高约3.8%，结构/覆盖率mean提高到`0.5170/0.1847`，三档CV仍为`0.044/0.059/0.089`。这是真实的背景波动下降与前景增强，不是显示后处理；但最弱轴向通道`(0,1)`仍受倍频耦合限制，背景散斑尚未完全消失。统一2×对比位于`output/falcons_5x5_cnr_background_comparison`。

2026-09-06起，SNR输出改为论文口径`20log10(mu_fg/sigma_bg)`；训练使用的背景扣除CNR保持不变。用新口径复算阶段15得到SNR mean/median/min/max=`6.53/6.71/4.64/7.36 dB`，25/25通道均高于0 dB，结果位于`output/falcons_5x5_background_suppression_stage15_paper_snr`。上段及更早文档中的`0.22/-3.08 dB`等SNR数值是旧的`20log10(abs(CNR))`口径，仅用于历史相对比较，不能与论文值直接横比。

优化器现支持`--paper-snr-weight`与`--paper-snr-target-db 15`，以逐通道论文SNR短缺的均值加最差通道短缺作为直接训练项；15 dB对应`mu_fg/sigma_bg > 5.6234`，验收严格使用`SNR_min > 15 dB`。从阶段15热启动并依次提高权重、移除CV等辅助项、增大学习率后，最终纯门槛压力测试`output/falcons_5x5_paper_snr15_linear_stage22_1500`达到论文SNR mean/min/max=`11.59/10.97/14.97 dB`，但大于15 dB仍为`0/25`。结构mean/min与覆盖率mean/min提高到`0.5396/0.5270`和`0.4254/0.4001`，代价是灰度单调降至`18/25`、三档CV恶化为`0.135/0.112/0.120`。因此该结果仅是SNR极限候选，未满足可用门槛；阶段15仍是当前结构、灰度一致性与背景抑制的平衡推荐。完整阶段命令、指标与结论见[falcons中心对称线稿的5×5/6×6级次选择](doc/falcons-order-grid-selection.md)。

按论文Figure S9的钟表方式新增3×3九通道迁移：不是9张重复图，而是先将完整falcons图整体左右镜像，再切成9个相邻角谱区域。当前纯迂回相位可执行级次取`m=-3…-1、n=2…4`，共同入射`theta_i=59.92067°、phi_i=-56.30993°`；9/9中心传播，零级和共轭对均为0，只有2对同方向倍数关系，最复杂3块全部无倍频。`P=2000 nm`下标称圆形FoV为`42.18116°`，不能照搬论文Sample 3在`P=1000 nm`下的87°。100轮纯主损失结构mean/min为`0.2902/0.2691`、覆盖率`0.0599/0.0526`、三档CV`0.454/0.410/0.374`，早期指标优于25通道100轮，但统一曝光仍是散斑，尚未形成可辨鹰脸。详细论文边界、级次映射和结果见[falcons中心对称线稿的5×5/6×6级次选择](doc/falcons-order-grid-selection.md)。

### 2026-09-05 当前结果：圆形山水图连续无零级6×6级次搜索

已按亮线密度与边缘总变差等权计算36个图块复杂度，并枚举`m_start,n_start∈[-12,12]`的全部连续6×6起点；包含`(0,0)`的方阵立即淘汰，只保留`P=2000 nm`理论条件下能由共同自由空间入射实现36/36中心传播的40个方阵。允许的操作仅为连续平移及整个6×6映射的旋转/镜像，不交换单块，也未使用此前100轮成像结果。

综合固定`lambda=480 nm、P=850 nm`下的最大传播数、完整本原方向倍数组、复杂度加权负担和轴向三倍惩罚，推荐仍为`m=-6…-1、n=-3…2`的原始映射。它在850 nm下以`theta_i=63.226°、phi_i=-18.430°`传播10/36个中心；在2000 nm理论对照下以`theta_i=58.052°、phi_i=8.130°`传播36/36个中心，圆形FoV为92.109°。最复杂前4块全部无倍频，最复杂12块中6块无倍频；主要风险仍是高复杂源图块`(5,4)`映射到6成员轴向组中的`(-2,0)`。850 nm下按空气锥裁切公式得到的180°只是几何上限，不能解释为完整36图块均可传播。

论文23-channel集合已按`lambda=488 nm、P=2000 nm、theta_i=46°、phi_i=0°`复现为23/23传播，三个抽查级次的出射角与补充材料Table S3一致。完整前5名、示意图、逐图块组成员及人工复核见[圆形山水图连续无零级6×6级次搜索](doc/circular-6x6-order-search.md)。本阶段没有启动1000/2500轮长时间图像优化，等待用户确认级次组合。

### 2026-09-05 当前结果：连续无零级6×6圆形山水图1000轮基线

使用严格行列映射 `m=-6…-1、n=-3…2` 完成36通道、1000轮纯主结构损失基线。输入圆图整幅等分为6×6，最近邻缩放后仍严格保留 `0/85/170/255 → 0/1/3/2/3/1`，没有块内重新分级。输出 `pairMat` 已确认与指定36级次逐项一致且不含 `(0,0)`。结构相似度 mean/min 为 `0.4372/0.2610`，覆盖率 mean/min 为 `0.2015/0.0501`，三档比例为 `0.344:0.678:1`，三档跨通道CV为 `2.431/2.455/2.454`。统一2×曝光下完整圆框和主要山水元素可辨，但整体偏暗且散斑明显。

`n=0` 六通道的结构/覆盖率/1×前景亮度均值为 `0.3642/0.0977/333247.33`，其余30通道为 `0.4518/0.2223/905377.24`；但差异未达到单侧Mann–Whitney 0.05显著性，并且对照均值被最外两列拉高。与相邻内列 `n=-2,-1,1` 比较时，n=0仅低约 `4.4%/4.0%/1.7%`，不能认定n=0整列独有地显著劣化。高复杂度 `(-2,0)` 仍偏弱，结构/覆盖率/亮度为 `0.3179/0.0544/263560.62`。n=0六级次共享同一 `dx` 且构成整数倍组，因此其独立性限制来自确定性的倍频耦合；1000轮末损失仍下降，说明全局尚未完全收敛，但增加轮数不能解除该耦合。

该组合值得保留为固定结构下的连续无零级数值基线，不建议作为最终36通道物理方案：850 nm条件下36级次无法全部进入空气锥，且当前三档跨通道CV很高。完整命令、逐级次表、输出路径和判断依据见 [连续无零级6×6圆图基线报告](doc/circular-6x6-nozero-baseline.md)。

随后从上述基线累计追加3300轮分段热启动，依次测试中等均衡、加强均衡、严格CV、结构恢复和阈值精调。严格低CV点可达到三档CV `0.0066/0.0395/0.0422`，但结构/覆盖率均值仅为 `0.310/0.059`。当前满足三档CV全部低于0.1时结构最好的折中结果为 `output/circular_6x6_cv_stage7_balanced_below_01_300`：三档CV `0.0587/0.0933/0.0847`，整体亮度CV `0.0427`，最暗/平均亮度 `0.910`，结构 mean/min `0.3459/0.0599`，覆盖率 mean/min `0.0741/0.0013`，三档比例 `0.403:0.694:1`。相对原始结构基线，结构均值下降约20.9%、覆盖率均值下降约63.2%，说明无法把一致性和结构指标同时推到各自最优。

最终折中结果中n=0列结构均值为 `0.3642`，高于n≠0的 `0.3422`；高复杂度 `(-2,0)` 改善到结构 `0.3910`、覆盖率 `0.0908`。因此整数倍关系限制独立调节能力，但不意味着含零坐标的通道必然无法成像。各阶段共用尺度的统一2×曝光对比位于 `output/circular_6x6_cv_stages_full_scene_2x`，完整权重、指标和结论已追加到[连续无零级6×6圆图基线报告](doc/circular-6x6-nozero-baseline.md)。

### 2026-08-31 当前结果：36通道几何线稿

输入为 `input/geometric_network_reference.png`，目标为 `output/geometric_network_target_36/thin_letter_6x6_target.mat`。1000轮结构基线的结构相似度均值为 `0.492`、覆盖率均值为 `0.352`，但三档跨通道CV约为 `0.726/0.732/0.735`。随后只通过损失权重和热启动进行4段各500轮的CV优先收尾，当前候选为 `output/geometric_network_36_cv_stage4_500`：

- 结构相似度均值/最差：`0.358/0.232`；前景覆盖率均值/最差：`0.162/0.089`。
- 36/36通道灰度单调；比例RMSE均值/最差：`0.038/0.199`。
- 深灰/中灰/亮灰跨通道CV：`0.054/0.133/0.180`。
- 背景像素CV均值 `1.001`，P95/均值 `2.996`，逐行CV `0.051`。

深灰档已接近用户期望的 `0.05`，但中灰和亮灰未达到；继续强压CV会进一步降低结构和可见亮度，因此没有把该结果描述为“三档均达到0.05”。限制主要来自36个第一象限级次中的整数倍谐波耦合，而不是显示缩放。固定物理前向、共享 `dx/dy` 和统一基础权重均未改变。

在此候选上先完成500轮温和公共亮度校准，再完成两段各500轮的强化增亮，最终结果位于 `output/geometric_network_36_brightness_q2_strong_stage2_500`。只把所有通道共用的最高档目标从 `Q=1` 提高到 `Q=2`，并提高已有档位下限、结构完整和前景能量损失权重；没有逐通道增益。相对增亮前，三档有效亮度由 `0.399/0.823/1.262` 提高到 `0.479/1.013/1.580`，增幅约为 `20.2%/23.0%/25.2%`；结构相似度由 `0.358` 提高到 `0.403`，覆盖率由 `0.162` 提高到 `0.223`，三档CV为 `0.052/0.116/0.150`。这证明真实增亮可行，但继续逼近 `Q=2` 仍会受到36级次耦合和固定总能量约束。

随后用该结果验证统一提高入射光强。公共倍率为 `1×/2×/4×` 时，平面均值及三个灰度档的原始有效亮度严格按倍率增加；三档CV始终为 `0.052/0.116/0.150`，相对灰度比例始终为 `0.311:0.646:1`。因此在当前线性模型中，提高照明强度可以让整幅图同步变亮，但不会改善或破坏相对灰度关系、通道一致性和结构。

用户随后取消优化器的绝对增亮目标，最终只保留灰度比例/间隔、跨通道CV、结构完整和前景覆盖约束，并在输出端统一使用物理入射光强 `2×`。两段各500轮CV收尾后的推荐结果为 `output/geometric_network_36_cv_gap_stage2_500`：三档CV为 `0.009/0.049/0.078`，比例RMSE为 `0.0181`，平均相邻差值为 `0.460/0.485`，36/36通道单调；结构相似度均值/最差为 `0.383/0.263`，覆盖率均值/最差为 `0.198/0.147`。相对上一候选，结构均值下降约5%、覆盖率均值下降约11%，但最差覆盖率提高约20%，背景指标基本不变。最终 `2×` 固定曝光验证位于 `output/geometric_network_36_cv_gap_stage2_global_2x`。

### 2026-09-03 当前结果：`block_test.png` 1000轮结构基线

新输入 `input/block_test.png` 为5000×5000单通道图，像素值严格为 `0/85/170/255`。使用 `create_thin_letter_target.py` 按6×6网格直接切为36个500×500通道，并将三档非零灰度映射为 `1/3、2/3、1`；目标位于 `output/block_test_target_36`。检测参数为 `--background-delta 1 --min-line-intensity 1 --level-assignment source`，用于完整保留最暗的85灰度档。

随后从随机相位执行1000轮纯主图案损失结构基线，结果位于 `output/block_test_36_main_1000`。结构相似度均值/最差为 `0.445/0.225`，前景覆盖率均值/最差为 `0.120/0.058`；36/36通道灰度单调，比例RMSE均值/最差为 `0.0161/0.0370`；三档跨通道CV为 `0.491/0.474/0.471`；背景像素CV均值为 `1.000`，P95/均值为 `2.994`，逐行CV为 `0.053`。统一曝光预览中多数通道轮廓可辨，但整体偏暗且低编号通道较弱，因此该结果仅作为新图可行性基线，尚未进行CV优先或结构完整性热启动收尾。

在该基线上继续完成三段各500轮的联合热启动。第一段用 `cross-level=50、gray-ratio=30、gap=2、structure-completeness=5、foreground-efficiency=8` 快速压低三档CV；第二段把 `cross-level` 降至20，并把结构完整和前景效率提高到15；第三段进一步改为 `cross-level=5、gray-ratio=20、gap=1、structure-completeness=30、foreground-efficiency=30`，在允许CV适度回升的前提下恢复结构。学习率依次为 `5e-4、3e-4、2e-4`，没有启用固定绝对亮度、逐通道增益或显示后处理。

第三段结果 `output/block_test_joint_stage3_500` 是当前结构/CV折中候选：结构相似度均值/最差为 `0.458/0.275`，相对基线同时提高约 `2.8%/22.1%`；前景覆盖率均值/最差为 `0.117/0.086`；36/36通道灰度单调，比例RMSE均值为 `0.0284`；三档CV为 `0.134/0.157/0.158`，相对基线下降约 `72.6%/66.9%/66.4%`。如果只优先追求最低CV，第二段 `output/block_test_joint_stage2_500` 可达到 `0.029/0.057/0.065`，但结构均值为 `0.440`，略低于基线，因此不作为当前综合推荐结果。

为增强肉眼灰阶差异，主脚本新增可选参数 `--desired-gray-ratios LOW MID`，只改变三个输入标签对应的期望物理响应，不改变输入MAT标签、固定前向或通道映射。以阶段3为初值，使用 `--desired-gray-ratios 0.15 0.55`、`cross-level=20、gray-ratio=80、structure-completeness=30、foreground-efficiency=30` 完成500轮后，实际平均响应比例由 `0.376:0.682:1` 拉开到 `0.214:0.576:1`。再用 `line-uniformity=0.5` 完成500轮温和收尾，当前强灰差候选为 `output/block_test_gray_gap_uniform_stage2_500`：实际比例为 `0.199:0.569:1`，三档跨通道CV为 `0.025/0.090/0.109`，结构相似度均值/最差为 `0.430/0.338`，36/36通道保持单调。三档线内像素CV由收尾前的 `0.890/0.752/0.638` 降至 `0.884/0.742/0.626`；均匀性有所改善，但固定相位前向下的散斑仍明显，不能等同于原始数字图中完全平坦的三档灰色。公共 `2×` 入射光结果位于 `output/block_test_gray_gap_uniform_stage2_global_2x`，只提高绝对亮度，不改变上述比例和CV。

本轮聊天产生的6个主要优化阶段已统一生成完整场景左右对比，位于 `output/block_test_full_scene_comparisons_2x`。全部右图使用公共 `2×` 入射光和共同显示尺度，不做逐结果美化；总览文件为 `all_stages_original_vs_optimized.png`。

针对1000轮结构基线中“不同channel整块亮暗不一”的问题，改从 `output/block_test_36_main_1000` 重新热启动，仅使用整体前景亮度一致性与最暗channel约束，不使用强三档跨通道约束。第一段500轮使用 `brightness-consistency=5、worst-channel=2、gray-ratio=10、structure-completeness=5、foreground-efficiency=5`；第二段500轮将前两项提高到 `10/5` 并把学习率降至 `1e-4`。最终结果 `output/block_test_channel_brightness_stage2_500` 的整体channel亮度CV由 `0.142` 降至 `0.0815`，最暗/平均亮度由 `0.785` 提高到 `0.878`；结构相似度均值/最差由 `0.445/0.225` 变为 `0.451/0.252`，没有通过压低结构换取一致性。三档CV同时由 `0.491/0.474/0.471` 降至 `0.258/0.296/0.304`。基线、第一段和第二段的公共 `2×` 完整场景对比位于 `output/block_test_channel_brightness_comparisons_2x`。

在整体channel亮度CV已降至 `0.0815` 后，进一步以“三档CV全部低于0.1”为目标完成两段各500轮热启动。第一段加入 `cross-level=20`，得到三档CV `0.053/0.122/0.146`；第二段使用 `brightness-consistency=10、worst-channel=5、cross-level=50、gray-ratio=20、structure-completeness=20、foreground-efficiency=20`，学习率降至 `5e-5`。最终候选 `output/block_test_channel_and_level_cv_stage2_500` 的三档CV为 `0.023/0.066/0.084`，整体channel亮度CV为 `0.0269`，最暗/平均亮度为 `0.953`，36/36通道灰度单调。结构相似度均值/最差为 `0.432/0.282`：均值比最初1000轮基线低约3%，最差值提高约25%。公共 `2×` 对比位于 `output/block_test_cv_below_01_comparisons_2x`。

### 2026-09-04 当前结果：`block_25.png` 排除黑channel的2500轮实验

输入 `input/block_25.png` 为5000×5000、像素值 `0/85/170/255` 的三灰度线稿。6×6切分后，`(6,2)…(6,6)` 五个纯黑格完全排除，31个有效channel参与优化；低/中/高灰档分别存在于31/30/27个channel。目标位于 `output/block_25_target_active31_fixed`，使用固定灰度映射而非块内分位映射，缺失灰档不伪造。

累计2500轮由1000轮结构基线和三段各500轮CV热启动组成。第一段使用 `brightness-consistency=20、worst-channel=20、cross-level=100、gray-ratio=30、structure-completeness=20、foreground-efficiency=20`，学习率 `3e-4`；第二段依次为 `50、50、500、100、30、30`，另加 `gap=20`，学习率 `2e-4`；严格第三段依次为 `200、200、10000、300、20、20`，另加 `gap=100`，学习率 `1e-4`。结构基线 `output/block_25_active31_main_1000` 的结构相似度均值/最差为 `0.535/0.351`，三档CV为 `1.612/1.593/0.913`。严格CV候选 `output/block_25_active31_cv_stage3_strict_500` 最终达到 `0.0039/0.0143/0.0103`，整体channel亮度CV为 `0.0164`，最暗/平均亮度为 `0.968`，31/31有效channel灰度单调；结构相似度均值/最差降至 `0.344/0.150`。最低灰档已低于0.01，中档和最高档仍略高，因此不能宣称三档全部低于0.01。使用更强CV权重后中档下降已接近停滞，继续压缩预计主要损害结构。公共 `2×` 完整场景对比位于 `output/block_25_active31_2500_comparisons_2x`。

公共入射光验证位于 `output/block_test_joint_stage3_global_2x`。从 `1×` 提高到 `2×` 后，平面均值和三档原始有效亮度严格翻倍；三档CV保持 `0.134/0.157/0.158`，相对比例保持 `0.378:0.682:1`，结构也不变。由此确认 `2×` 适合用于最终绝对增亮，但不能代替损失函数改善结构或跨通道一致性。

### 2026-09-09 当前结果：`easy_picture.png` 3×3 九通道 SNR 极限

使用 `m,n=-3…-1`、identity 整体映射和固定共享 `dx/dy` 前向完成 A–F 分阶段 GPU 优化。目标严格保持 `0/85/170/255 -> 0/1/3/2/3/1`，`pairMat` 按 m 优先排列且不含 `(0,0)`。`lambda=480 nm`、`P_x=P_y=2000 nm`、空气和 `NA=1` 时，9/9 级次中心及完整局部角谱方格均在传播/收集范围内；成像结论仍来自数值优化，尚未经过器件实验验证。

阶段 F 平衡解位于 `output/easy_picture_3x3_stage_f_snr15_500`：结构 mean/min=`0.7368/0.7021`，覆盖率 mean/min=`0.6076/0.5665`，9/9 灰度单调，比例 RMSE mean/max=`0.0408/0.0536`，三档 CV=`0.0347/0.0332/0.0337`，整体 channel 亮度 CV=`0.0443`，论文 SNR mean/median/min/max=`12.42/12.29/12.05/13.37 dB`。它是当前结构、灰度一致性和背景噪声之间的平衡可用解，但 0/9 通道超过 15 dB。

移除 CV/背景辅助项并把线性 SNR hinge 权重提高到100后，纯 SNR 极限解 `output/easy_picture_3x3_stage_f_snr_limit_total_3500` 达到 9/9 通道严格 `SNR>15 dB`；最差为 `15.002610717607 dB`，对应 `(-1,-1)`。代价是结构 mean/min 降到 `0.6664/0.6080`，比例 RMSE mean/max 升到 `0.1503/0.2654`，最高灰档跨通道 CV 升到 `0.1047`；灰度单调仍为9/9，覆盖率 mean/min 为 `0.6404/0.5925`。因此该结果只能称为纯 SNR 极限解，不能替代平衡解。完整阶段表、SNR排序、逐通道指标、统一2×曝光图和命令记录见 [easy_picture 3×3级次分析](doc/easy-picture-3x3-clock-order-analysis.md)。
